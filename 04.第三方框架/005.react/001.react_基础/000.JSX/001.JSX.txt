

//======================================================================== JSX
//======================================================================== JSX

JSX 是 JavaScript XML 的缩写，意思是：

在 JavaScript 代码中写 " HTML 结构" 的语法。

它是 React 专用的语法，让你可以像写 HTML 一样写界面。

//==================== JSX 语法，属于 React 的核心语法之一。
//==================== JSX 语法，属于 React 的核心语法之一。

