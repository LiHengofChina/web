
<ProForm
	disabled={false}
	formRef={formRef}
	submitter={{ 			}}					//	用于配置表单底部的按钮（如“提交”、“重置”）。
	onFinish={async (value) => { 		}}		//	提交逻辑回调

	request={async () => {   			}}		//	request：用于回显（详情/编辑）数据
												//  应返回一个对象，作为表单的初始值。
	>

</ProForm>



