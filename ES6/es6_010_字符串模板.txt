//=======================
//=======================
//=======================
es6中允许使用 `` 创建字符串模板,可以直接写回车空格编写html或文本

	let abc = `
		<div>
			<p>我想去看卡${na}</p>
		 </div>
	`;
	$('body').html(abc);



//=======================
//=======================
//=======================
在字符串拼接中，可以代替 ' ' 以及  " " ，
在 ` ` 中可以使用 ${ }直接把变量与字符串拼接起来

	let demo="小红";
	console.log(`我的名字是：${demo}`);



//=======================
//=======================
//=======================
在${}中不仅可以进行运算，还可以使用三目运算符




