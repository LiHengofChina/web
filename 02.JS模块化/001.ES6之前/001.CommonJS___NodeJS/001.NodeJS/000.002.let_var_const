1). let 
	js规范ES6中，新增了let 命令，用来声明变量。用法类似于var

	let 所声明的变量，只在let 命令所在的代码块内有效。
		{ let a = 10; }
		
	var 所声明的变量，可以在声明之前使用，只不过是undefined


2). const
	const声明一个只读的常量。
	一旦声明，常量的值就不能改变。
	const PI = 3.1415;
	一旦声明了变量，就必须立即初始化	
