
//============================================================= npm 安装时升级不了。
//============================================================= npm 安装时升级不了。

Run npm install -g npm@8.5.3 to update!

需要删除和 npm 目录旁边的 _.npm-Yd9HtPuI 目录

另外
	node安装目录 //优先级最后
	全局目录      //优先级其次
	项目目录      //优先级最高

都有node_modules，一定要找对目录。

 
