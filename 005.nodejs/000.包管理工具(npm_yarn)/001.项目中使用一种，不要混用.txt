

一个项目建议只使用 Yarn 或 npm 中的一种，不要混用。




//================================ 为什么不能混用？
//================================ 为什么不能混用？

Yarn 会生成 yarn.lock

（1）锁文件不同：
	 npm 会生成 package-lock.json
	 → 混用可能导致依赖版本冲突、不一致

（2）缓存机制不同：
	 两者使用不同的缓存路径，混用可能重复下载或出错

（3）团队协作问题：
	 有人用 Yarn，有人用 npm，可能导致“运行没问题但部署挂掉”的情况


